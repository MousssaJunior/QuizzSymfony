{% extends 'base.html.twig' %}

{% block title %}Reponse index{% endblock %}

{% block body %}
    <h1>Reponse index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_question</th>
                <th>Reponse</th>
                <th>Reponse_expected</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reponse in reponses %}
            <tr>
                <td>{{ reponse.id }}</td>
                <td>{{ reponse.idQuestion }}</td>
                <td>{{ reponse.reponse }}</td>
                <td>{{ reponse.reponseExpected }}</td>
                <td>
                    <a href="{{ path('app_lambdareponse_show', {'id': reponse.id}) }}">show</a>
                    <a href="{{ path('app_lambdareponse_edit', {'id': reponse.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_lambdareponse_new') }}">Create new</a>
{% endblock %}
