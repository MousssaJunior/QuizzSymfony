{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Id_question</th>
                <td>{{ reponse.idQuestion }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ reponse.reponse }}</td>
            </tr>
            <tr>
                <th>Reponse_expected</th>
                <td>{{ reponse.reponseExpected }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lambdareponse_index') }}">back to list</a>

    <a href="{{ path('app_lambdareponse_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('lambdareponse/_delete_form.html.twig') }}
{% endblock %}
