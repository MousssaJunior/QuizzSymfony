{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Id_categorie</th>
                <td>{{ question.idCategorie }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adminquestion_index') }}"class="btn btn-primary btn-lg">back to list</a>

    <a href="{{ path('app_adminquestion_edit', {'id': question.id}) }}"class="btn btn-warning btn-lg">edit</a>

    {{ include('adminquestion/_delete_form.html.twig') }}
{% endblock %}
