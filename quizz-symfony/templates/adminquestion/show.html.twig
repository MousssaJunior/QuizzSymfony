{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Id_categorie</th>
                <td>{{ question.idCategorie }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adminquestion_index') }}">back to list</a>

    <a href="{{ path('app_adminquestion_edit', {'id': question.id}) }}">edit</a>

    {{ include('adminquestion/_delete_form.html.twig') }}
{% endblock %}
