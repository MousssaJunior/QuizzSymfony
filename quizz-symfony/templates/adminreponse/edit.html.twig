{% extends 'base.html.twig' %}

{% block title %}Edit Users{% endblock %}

{% block body %}
    <h1>Edit Users</h1>

    {{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.isAdmin) }} {# Afficher le champ isAdmin #}
    <button type="submit">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_adminusers_index') }}">back to list</a>
{% endblock %}

