{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_admincategory_index') }}"class="btn btn-primary btn-lg">Back to list</a>


    <a href="{{ path('app_admincategory_edit', {'id': categorie.id}) }}"class="btn btn-warning btn-lg">Edit</a>
    {{ include('admincategory/_delete_form.html.twig') }}
{% endblock %}
